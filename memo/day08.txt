추상 클래스
	필드 안에 구현이 안 된 메소드가 선언되어 있는 클래스를 추상 클래스라고 한다.
	이 때 구현되지 않은 메소드를 추상 메소드라고 부른다.
	반드시 재정의를 통해 구현을 해야지만 메모리에 할당되기 때문에
	"강제성"을 부여하기 위해서 추상 메소드로 선언한다.

* 추상클래스로 만들어야지만 추상메소드도 선언할 수 있다. 추상클래스는 필드 안에 추상적인 것을 선언하기 위해서 추상클래스라고 한다. 


추상 클래스 선언
	abstract class 클래스명 {
		abstract 리턴타입 메소드명(매개변수, ...);
	일반 메소드 선언가능.
	}
-----------------------------------------------------------------------------------------

인터페이스(interface) : 틀(클래스와는 다른 개념)// 추상클래스를 고도화 시킨 문법(cf.추상클래스는 일반 메소드도 섞여있음. 인터페이스는 추상메소드만 선언 가능)
	추상 클래스를 고도화시킨 문법.
	상수와 추상메소드만 존재한다.
	구현은 지정한 클래스에서 진행하고, 인터페이스를
	다른 클래스에 지정할 때에는 inplements 키워드를 사용한다.
	* 부모클래스의 역할을 함
	* 변수 선언 불가능, 변수형태로 선언 시 static과 final이 생략됨
	static: 컴파일러가 메모리에 할당하기 위해서 new가 아니라 static이 지정됨. 객체단위로 사용이 아니라.. '공유'하기 위해서 

추상 클래스와 인터페이스 간의 관계
	인터페이스를 클래스에 바로 지정하면 모든 메소드에 강제성이 부여되어서
	전부 다 구현해야 한다. 하지만 일반적인 상황에서는 모든 것이 아닌,
	필요한 메소드를 골라서 재정의해야한다.
	인터페이스를 직접 지정하지 않고 다른 클래스에 지정한 후 바디를 만들어 놓는다면,
	강제성이 소멸되고 이 클래스를 상속받아서 필드를 구현한다면, 골라서 재정의할 수 있게 된다.
	이 때 중간에서 강제성을 없애주는 클래스를 추상클래스로 선언하기로 하며,
	추상클래스 이름 뒤에는 Adapter를 붙여서 목적을 알려준다.

-------------------------------------------------------------------------------------------

마커 인터페이스(Maker Interface)
	클래스들을 그룹화하기 위한 목적으로 사용한다.
	인터페이스는 지정한 클래스의 부모이며, 모든 자식은 부모의 타입이므로
	마커 인터페이스를 지정받은 클래스들이 하나의 타입으로 묶이게 된다.
	이름 뒤에 Marker를 붙여줘야 한다.
*타입 구분은 instanceof 를 사용

-------------------------------------------------------------------------------------------

내부 클래스(Inner Class) (주로 안드로이드에서 사용) // 면접 때 많이 나옴 
	하나의 클래스에서 a작업과 b작업이 있을 때에는 따로 분리하여 클래스로 만들지 않고,
	클래스 안에 클래스를 선언하여 설계한다. 이 때 밖에 있는 클래스를 외부 클래스라고 하며,
	안에 선언된 클래스를 내부 클래스라고 한다. 외부 클래스가 메모리에 할당되어야
	내부 클래스를 객체화할 수 있기 때문에 클래스를 숨기기 위해서 내부 클래스를 사용하기도 하며,
	이를 *(면접 때 많이 나옴) 캡슐화 또는 은닉화라고 한다. 내부 클래스는 외부 클래스의 필드이기 때문에
	외부 클래스의 필드를 자신의 필드처럼 가져다 사용할 수 있지만 상속관계가 아니기 때문에 
	내부 클래스일지라도 extends를 통해 다른 클래스를 상속받아서 사용할 수 있다.
	(외부 클래스와 private 접근자) 

->  어떤 영역 안에서 클래스가 선언되면 무조건 내부 클래스라고 부른다. 

익명 클래스(Anonymous Inner Class)
	이름이 없는 클래스이며 구현되지 않은 필드를 구현하기 위해 일회성으로 생성되는 클래스이다.

			//구현이 안 되어 있어서 오류 발생. 메모리에 안 올라가 있음. 
			//위의 중괄호는 클래스 중괄호, 그러나 이름이 없음. 
			//게임 인터페이스를 메모리에 올린 것이 아니라 game 클래스를 올린 것임. 이게 왜 업캐스팅이야? ★이거에 대해 내가 이해한 설명 써놓기 (computer.java)

			
			//인터페이스에 구현되지 않은 필드가 존재하기 때문에,
			//익명 클래스가 열린 뒤 인터페이스를 지정하여 구현시킨다.
			//구현된 익명 클래스의 필드가 메모리에 할당된 뒤 인터페이스 타입의
			//객체로 up casting된다.

-----------------------------------------------------------------------------------------

심화 실습(클래스 ~ 익명 클래스)

- 스타벅스 본사 클래스 선언(register(Form form)) -// > 안에 등록하기 메소드 // -> 폼의 인터페이스 만드기 // -> 추상메소드 메뉴판, 판매방식 / 
 메뉴 전달받아서, 메뉴가 잇는지 검사하고 있으면 판매완료 
- 본사 제출 양식 인터페이스 선언 //
- 건물 클래스 선언 (메인 메소드)  //

스타벅스 지점 오픈 시 본사에 양식을 작성하여 제출
- 제출 양식에는 메뉴판(getMenu())과 판매 방식(sell(String menu))을 작성한다. //
- 강남점은 메뉴 그대로 판매할 수 있어야 하고, 잠실점은 무료 나눔 행사중이다. 
- 무료 나눔 행사중인 지점은 판매방식이 따로 없다.

등록하기 메소드 사용 / 지점에서 등록하기 메소드 사용 / 메소드의 매개변수: FORM (본사에서 틀은 짜놓아도 구현은 할 수 없음) / 인터페이스 
객체를 만들면 지점, 지점 만들면 본사에 알려야함. 
사용하는 쪽에서 form을 구현해야함, 사용하는 순간 익명 클래스가 열림 
잘 전달햇는지 검사 instanceof 

-메뉴판 출력 (문자열 배열로 리턴)
-판매 방식 void //

빌딩, new starbucks
잠실.register(form,{익명 클래스 열림 -> 재정의 }

void register(Form form){
}

-강남점은 익명클래스 구현
-잠실점은 adapter를 사용하여 구현(추상클래스) 

1. 스타벅스 본사 클래스
2. 스타벅스 클래스 안의 등록하기 메소드 
3. 본사 제출 양식 인터페이스 선언 form
 - (getmenu())와 판매 방식 (sell(String menu))을 작성한다.
 
4. 건물 클래스 선언 main 메소드
 - 지점을 객체로 선언.
 - 

- gangnam.register(form -> 등록완료. 
- jamsil.register(form -> 무료나눔 행사중입니다. 

 - 사용할 때 form 구현, 사용할 때 익명 클래스를 열기. (상속 받고 








1. 스타벅스 본사 클래스 만들기
-공용필드: 레지스터 만들기 

2. form
string[] getMenu();
void sell;




나이키 본사 클래스 선언
- 나머지는 위와 동일(잠실 정상, 강남 무료 나눔)

=======================================================
다중 상속
(*자바는 다중 상속을 지원하지 않는다)

	여러 부모 클래스를 상속하는 것을 다중 상속이라고 한다.
	JAVA는 모호성 때문에 다중 상속을 지원하지 않는다.
	하지만 JDK8버전부터는 인터페이스에 default 메소드를 선언할 수 있으며,
	여러 개를 지정할 수 있는 인터페이스 특성상 다중 상속을 지원하는 것이나 다름이 없다.
*abstract 자리에 default 라고 적으면 일반 메소드 사용 가능(왜냐하면 ab 가 생략되어 있는 값이기 때문에) 

모호성(ambiguity)
	하나의 자식이 여러 부모를 상속받을 때 부모 필드에 동일한 이름의 필드가 있다면,
	어떤 부모의 필드인지 알 수가 없다. 이를 모호성이라고 부른다.

모호성 해결 방법
	-상황 1: 두 개의 인터페이스 내에 이름과 매개변수가 똑같은 메소드가 선언되어 있다.
	-해결 1: 자식 클래스에서 재정의하여 사용한다. 
	           원하는 부모의 필드에 접근하기 위해서는 재정의된 메소드에서 "부모명.super.필드명"을 작성한다.

	-상황 2: 부모 클래스의 메소드와 인터페이스의 디폴트 메소드의 이름과 매개변수가 똑같이 선언되어 있다.
	-해결 2: 부모 클래스의 메소드가 사용된다.
=======================================================
함수형 인터페이스(Functional interface)
	인터페이스 중 추상 메소드를 하나만 가지고 있는 인터페이스를 함수형 인터페이스라고 한다.
	이 때 @FtuntionalInterface를 인터페이스위에 작성하여 단 하나의 추상메소드만
	선언할 수 있도록 제한해야 한다.


람다식(익명 메소드)(Lamda Expression)
	이름이 없는 메소드로서 변수처럼 사용이 가능하며, 매개변수로도 전달이 가능하다.
	함수형 인터페이스는 추상 메소드가 한 개만 선언되기 때문에 메소드 이름이 필요 없다.
	따라서 람다식을 익명 메소드(Anonymous Method)라고도 부른다.

람다식 문법
	1. (매개변수 형식 나열, ...) -> 리턴값;
	EX) (number) -> number % 10 == 0; 
	2. (매개변수 형식 나열, ...) -> { 2개 이상의 문장 작성; return 리턴값; }
	3. 매개변수가 한 개일 경우에는 소괄호 전부 생략 가능

=======================================================

<숙제 :: 내일까지 하기>
람다식 기초 실습
	PrintName 인터페이스 선언(함수형 인터페이스)
	getfullName() 추상 메소드 선언
	- 성과 이름을 받는다.
	*상황에 맞춰서 구현 
	PrintNameTest 클래스 선언
	printFullName() 메소드 선언 (이름을 합쳐서 출력)
	- 외부에서 구현된 PrintName 을 전달받은 뒤 전체 이름을 출력
--잘 모르겠으면 익명클래스 열어서 람다식으로 줄이는 방법 생각하기.



=======================================================

[심화 실습]
여러 개의 정수를 입력받아서 알맞는 덧셈, 뺄셈 결과를 확인하는 애플리케이션 제작
//결합성으로 정수를 받아서 
입력 예1) 7 + 35 - 9
출력 예1) 33

입력 예2) -9 + 8 + 10
출력 예2) 9

* "ABC".split("")은 [A][B][C] 3칸 문자열 배열로 리턴된다.
   "A,B,C".split(",")은 [A][B][C] 3칸 문자열 배열로 리턴된다.
   split("구분점")을 전달하면 문자열에서 동일한 구분점을 기준으로 문자열 값을 잘라낸 후 문자열 배열로 리턴한다.
   구분점을 여러 개 사용할 때에는 split("구분점|구분점")으로 사용하며, "+", "-"를 구분점으로 사용할 때에는 "\\+", "\\-"로 표현한다.
   예)"4 + 9".split("\\+")은 [4][9] 2칸 문자열 배열로 리턴

* 사용자가 정상적으로만 입력한다는 가정 하에 구현하도록 한다.
* 두 정수를 전달받은 후 int로 리턴하는 calc 추상메소드 선언(함수형 인터페이스 제작)
* 두 정수의 덧셈, 뺄셈을 구해주는 함수형 인터페이스를 리턴하는 static 메소드 선언(람다식 리턴)
* 전체 식을 전달받은 후 String[]로 리턴하는 getOpers 추상메소드 선언(함수형 인터페이스 제작)
* main메소드에 getOper를 람다식으로 구현
* 첫번째 정수가 음수일 경우 오류 해결
